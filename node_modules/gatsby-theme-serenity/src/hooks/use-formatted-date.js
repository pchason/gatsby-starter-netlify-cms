import moment from "moment";

export default function useFormattedDate(date, format="DD-MM-YYYY") {
    const m = moment(date).utc().format("YYYY-MM-DD HH:mm:ss");
    const dateFormatted = m.format(format);
    return dateFormatted;
}